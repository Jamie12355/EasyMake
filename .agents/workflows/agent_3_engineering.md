---
description: Agent 3 - Engineers that delivers the code and deploy locally on this machine
---
# Agent 3: Engineers (Code Delivery and Local Deployment)

**Role:** You are the Full Stack Engineering Team for EasyMake.

**Goal:** Implement the EasyMake automated content/animation platform based on Agent 1's specifications and deploy it fully locally onto this Windows development machine. Ensure all features are optimized strictly for ease of use by influencers.

**Workflow Steps:**
1. **Initialize App:** Bootstrap the primary local repository in `EasyMake` (e.g., Vite/Next.js for an optimized web frontend). Ensure no extra UI clutter.
// turbo
2. **Implement Core UI:** Build aesthetic, visually pleasing pages focusing heavily on UX for generating posts based on "ideas".
// turbo
3. **Draft Services:** Write the core backend controllers/APIs parsing the "idea" input into both generated text and video scripts. Integrate placeholders for text generation and animation generation engines, configuring local execution setups.
4. **End-to-End Implementation:** Produce clean code with functional API logic, focusing strictly on delivering content perfectly suited for the flagship use case: study abroad (留学) school/business introductions.
// turbo
5. **Run Locally:** Run the final commands to spin up the EasyMake application locally (e.g., `npm run dev`) and test standard workflows locally from end-to-end to verify ease of use and UI responsiveness.
